
import 'package:flutter/material.dart';

class BotonCajaDecorativa extends StatelessWidget {
  const BotonCajaDecorativa({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}